import { BoolToCadenaPipe } from './bool-to-cadena.pipe';

describe('BoolToCadenaPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolToCadenaPipe();
    expect(pipe).toBeTruthy();
  });
});
